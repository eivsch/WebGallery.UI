@model WebGallery.UI.ViewModels.Bio.BioPictureViewModel

@{
    string returnUrl = Model.AlbumMediaIndex > 0 ? $"/bio/{Model.AlbumMediaIndex}" : "/bio";
}

<p>Deleted!</p>
<span class="btn btn-dark btn-sm">
    <a href=@returnUrl>Return</a>
</span>
